# ============================================================================
# Staging Environment Configuration
# ============================================================================
#
# Copy this file to .env.staging and update values
# cp .env.staging.example .env.staging
#
# Usage:
#   docker-compose -f docker-compose.staging.yml --env-file .env.staging up -d
#

# ============================================================================
# REQUIRED - Must be changed from defaults
# ============================================================================

# PostgreSQL Password
POSTGRES_PASSWORD=staging_secure_password_change_me

# Security Secrets (generate with: openssl rand -base64 32)
JWT_SECRET=staging_jwt_secret_MUST_BE_32_CHARS_OR_MORE_change_me
SESSION_SECRET=staging_session_secret_MUST_BE_32_CHARS_OR_MORE_change_me

# CORS Origins (comma-separated, no wildcards)
ALLOWED_ORIGINS=https://staging.stockmind.example.com

# Base URL
STAGING_BASE_URL=https://staging.stockmind.example.com

# ============================================================================
# OPTIONAL - AI API Keys
# ============================================================================
# These can be set via UI after first login, but setting here is faster for testing

# ANTHROPIC_API_KEY=sk-ant-...
# OPENAI_API_KEY=sk-...
# ELEVENLABS_API_KEY=...
# HEYGEN_API_KEY=...

# ============================================================================
# OPTIONAL - Instagram Graph API
# ============================================================================

# FB_APP_ID=your-facebook-app-id
# FB_APP_SECRET=your-facebook-app-secret

# ============================================================================
# OPTIONAL - Monitoring
# ============================================================================

# Sentry (separate DSN for staging)
# SENTRY_DSN_STAGING=https://your-sentry-dsn@sentry.io/project-id-staging
# VITE_SENTRY_DSN_STAGING=https://your-sentry-dsn@sentry.io/project-id-staging

# ============================================================================
# OPTIONAL - Feature Flags
# ============================================================================

# Allow new user registration (default: true for staging)
ENABLE_REGISTRATION=true

# Git commit for version tracking
GIT_COMMIT=latest

# ============================================================================
# NOTES
# ============================================================================
#
# 1. Database data stored in: ./data/postgres-staging
# 2. Uploads stored in: ./uploads-staging
# 3. Backups stored in: ./backups-staging
# 4. Staging runs on port 5001 (production on 5000)
# 5. Database on port 5433 (production on 5432)
#
# Test staging:
#   curl http://localhost:5001/health
#   docker-compose -f docker-compose.staging.yml logs -f app-staging
