# 📊 КАК РАБОТАЕТ ОЦЕНКА СТАТЕЙ

## 🔄 АВТОМАТИЧЕСКАЯ ОЦЕНКА (ДО АНАЛИЗА)

### Когда происходит:
1. **При парсинге RSS** - когда статьи загружаются из RSS источников
2. **При открытии списка статей** - если у статьи нет оценки, она оценивается автоматически в фоне

### Как работает:

#### 1. Триггер автоматической оценки
```typescript
// server/routes/news.routes.ts (строки 36-43)
// При загрузке списка статей проверяется:
const itemsWithoutScore = items.filter(item => item.aiScore === null);
if (itemsWithoutScore.length > 0) {
  // Запускается фоновая задача
  scoreRssItems(itemsWithoutScore, userId)
}
```

#### 2. Фоновая оценка
```typescript
// server/routes/helpers/background-tasks.ts (строки 191-262)
// Для каждой статьи БЕЗ оценки:
for (const item of items) {
  const result = await scoreNewsItem(
    apiKey.decryptedKey,
    item.title,        // Заголовок статьи
    item.content       // Краткое содержание из RSS (первые 3000 символов)
  );
  
  // Сохраняется в БД:
  await storage.updateRssItem(item.id, {
    aiScore: result.score,      // Оценка 0-100
    aiComment: result.comment    // Комментарий AI
  });
}
```

#### 3. AI функция оценки
```typescript
// server/ai-services/score-news.ts
// Использует простой промпт для быстрой оценки:

Промпт:
"Оцени вирусный потенциал статьи для коротких видео (0-100):
- 90-100: Очень вирусная
- 70-89: Высокий потенциал
- 50-69: Средний потенциал
- 0-49: Низкий потенциал"

Возвращает:
{
  score: 0-100,
  comment: "Краткое объяснение на русском"
}
```

### Характеристики автоматической оценки:
- ✅ **Быстрая** - использует короткий промпт (MAX_TOKENS_SHORT)
- ✅ **Простая** - только общая оценка вирусности
- ✅ **Фоновая** - не блокирует интерфейс
- ✅ **Автоматическая** - запускается сама при парсинге
- ⚠️ **Ограниченная** - анализирует только title + первые 3000 символов

---

## 🔍 РУЧНОЙ АНАЛИЗ (КНОПКА "АНАЛИЗ СТАТЬИ")

### Когда происходит:
- Пользователь нажимает кнопку "Анализ" на отдельной статье
- Или кнопку "Проанализировать все статьи"

### Как работает:

#### 1. Клиент отправляет запрос
```typescript
// client/src/components/project/stages/stage-2/hooks/use-news-analysis.ts
const analyzeMutation = useMutation({
  mutationFn: async ({ itemId, title, content }) => {
    // Сначала загружается полный текст статьи (если нужно)
    if (!item.fullContent) {
      const fullContentResponse = await apiRequest(
        "POST", 
        `/api/news/${item.id}/fetch-full-content`, 
        {}
      );
      content = fullContentData.content || content;
    }
    
    // Отправляется запрос на анализ
    const res = await apiRequest("POST", "/api/news/analyze", { 
      title, 
      content  // ПОЛНЫЙ текст статьи
    });
    return await res.json();
  }
});
```

#### 2. Сервер запускает продвинутый анализ
```typescript
// server/routes/news-analysis.routes.ts (строки 61-102)
app.post("/api/news/analyze", async (req, res) => {
  const { title, content } = req.body;
  
  // Используется ПРОДВИНУТАЯ система анализа
  const analysis = await scoreNewsAdvanced(apiKey, title, content);
  
  return apiResponse.ok(res, {
    ...analysis,  // Полный результат анализа
    metadata: {
      analysisTime: duration,
      timestamp: new Date().toISOString()
    }
  });
});
```

#### 3. Multi-Agent система анализа
```typescript
// server/ai-services/advanced.ts (строки 11-28)
export async function scoreNewsAdvanced(apiKey, title, content) {
  // Запускаются 4 агента ПАРАЛЛЕЛЬНО:
  const [hook, structure, emotional, cta] = await Promise.all([
    analyzeHook(apiKey, fullContent),           // Агент 1: Хук
    analyzeStructure(apiKey, fullContent),     // Агент 2: Структура
    analyzeEmotionalImpact(apiKey, fullContent), // Агент 3: Эмоции
    analyzeCTA(apiKey, fullContent)            // Агент 4: Призыв к действию
  ]);
  
  // Агент 5: Архитектор синтезирует результаты
  return synthesizeAnalysis(apiKey, hook, structure, emotional, cta, 'news');
}
```

### Детали каждого агента:

#### 🤖 АГЕНТ 1: HOOK EXPERT (Эксперт по хукам)
Анализирует первые 3-5 секунд контента:
- **Attention Grab** (0-100) - останавливает ли скролл?
- **Clarity** (0-100) - понятна ли тема за 1 секунду?
- **Specificity** (0-100) - есть ли конкретные цифры/факты?
- **Emotional Trigger** (0-100) - вызывает ли эмоции?
- **Pattern Match** (0-100) - соответствует ли вирусным паттернам?

**Примеры:**
- ❌ "Как заработать деньги" → 20-30 баллов (слишком общее)
- ✅ "Как я заработал $10,247 за 18 дней" → 90-95 баллов (конкретно)

#### 📐 АГЕНТ 2: STRUCTURE ANALYST (Аналитик структуры)
Анализирует структуру контента:
- **Pacing** - слова в минуту (WPM), оптимально ли?
- **Information Density** - фактов в секунду
- **Scene Flow** - логичная ли прогрессия?
- **Optimal Length** - оптимальная ли длина?

#### 💭 АГЕНТ 3: EMOTIONAL IMPACT ANALYST (Аналитик эмоций)
Анализирует эмоциональное воздействие:
- **Primary Emotion** - какая основная эмоция? (fear, greed, curiosity, anger, joy, fomo, pride)
- **Relatability** (0-100) - насколько это релевантно?
- **Shareability Score** (0-100) - захочется ли поделиться?
- **Pain Points** - какие болевые точки затронуты?

#### 🎯 АГЕНТ 4: CTA ANALYST (Аналитик призывов)
Анализирует призывы к действию:
- **Presence** - есть ли CTA? Какой тип?
- **Placement** - где размещен? (early/mid/end)
- **Effectiveness** - насколько эффективен?

#### 🏗️ АГЕНТ 5: ARCHITECT (Архитектор)
Синтезирует все анализы:
- **Overall Score** (0-100) - взвешенная средняя оценка
- **Verdict** - viral / strong / moderate / weak
- **Strengths** - топ-3 сильные стороны
- **Weaknesses** - топ-3 слабые стороны
- **Recommendations** - 3-5 приоритетных рекомендаций с конкретными изменениями
- **Viral Patterns** - какие паттерны найдены / какие отсутствуют
- **Predicted Metrics** - прогноз производительности

### Результат анализа:
```typescript
{
  overallScore: 75,           // Общая оценка 0-100
  verdict: "strong",          // viral / strong / moderate / weak
  confidence: 0.85,           // Уверенность 0-1
  
  // Оценки по агентам
  hookScore: 80,
  structureScore: 70,
  emotionalScore: 75,
  ctaScore: 65,
  
  // Детальный разбор
  breakdown: {
    hook: { /* детали хука */ },
    structure: { /* детали структуры */ },
    emotional: { /* детали эмоций */ },
    cta: { /* детали CTA */ }
  },
  
  // Что работает
  strengths: [
    "Сильный хук с конкретными цифрами",
    "Хорошая эмоциональная вовлеченность",
    "Четкая структура"
  ],
  
  // Что не работает
  weaknesses: [
    "Слабый призыв к действию",
    "Слишком быстрый темп в середине",
    "Недостаточно конкретики"
  ],
  
  // Рекомендации
  recommendations: [
    {
      priority: "critical",
      area: "cta",
      current: "Нет призыва к действию",
      suggested: "Добавить 'Подпишись для больше таких историй'",
      expectedImpact: "+15-20 points",
      reasoning: "CTA увеличивает engagement на 30-40%"
    }
  ],
  
  // Вирусные паттерны
  viralPatterns: {
    matched: [
      { pattern: "curiosity-gap", confidence: 85 }
    ],
    missing: [
      { pattern: "social-proof", potentialBoost: "+10-15 points" }
    ]
  },
  
  // Прогноз
  predictedMetrics: {
    estimatedRetention: "55-65%",
    estimatedSaves: "3-5% of viewers",
    estimatedShares: "1-2% of viewers",
    viralProbability: "medium-high"
  }
}
```

---

## 📊 СРАВНЕНИЕ

| Характеристика | Автоматическая оценка | Ручной анализ |
|----------------|----------------------|---------------|
| **Скорость** | ⚡ Быстро (1-2 сек) | 🐌 Медленно (10-30 сек) |
| **Детализация** | 📝 Простая (1 оценка) | 📚 Детальная (4 агента + синтез) |
| **Данные** | Только title + 3000 символов | Полный текст статьи |
| **Результат** | `{ score, comment }` | Полный анализ с рекомендациями |
| **Когда** | Автоматически при парсинге | По запросу пользователя |
| **Стоимость** | 💰 Дешево | 💰💰 Дорого (больше токенов) |
| **Цель** | Быстрая фильтрация | Глубокое понимание |

---

## 🎯 КОГДА ЧТО ИСПОЛЬЗОВАТЬ

### Автоматическая оценка:
- ✅ Для быстрой фильтрации статей
- ✅ Для сортировки по вирусному потенциалу
- ✅ При первом просмотре списка
- ✅ Для массовой оценки новых статей

### Ручной анализ:
- ✅ Когда нужно понять, почему статья хорошая/плохая
- ✅ Перед созданием проекта из статьи
- ✅ Для получения конкретных рекомендаций
- ✅ Для глубокого понимания контента

---

## 🔧 ТЕХНИЧЕСКИЕ ДЕТАЛИ

### Автоматическая оценка:
- **Модель**: Claude Sonnet 4.5
- **Токены**: MAX_TOKENS_SHORT (512)
- **Промпт**: Простой, короткий
- **Формат**: JSON с score и comment

### Ручной анализ:
- **Модель**: Claude Sonnet 4.5 (все агенты)
- **Токены**: MAX_TOKENS_LONG (3072) для каждого агента
- **Промпты**: Специализированные для каждого агента
- **Параллелизм**: 4 агента работают одновременно
- **Формат**: Сложная структура AdvancedScoreResult

---

## 💡 ПРИМЕРЫ

### Автоматическая оценка:
```
Статья: "Apple представила новый iPhone"
Оценка: 65/100
Комментарий: "Средний вирусный потенциал, интересно для tech-аудитории"
```

### Ручной анализ:
```
Статья: "Apple представила новый iPhone"

Overall Score: 68/100
Verdict: moderate

Strengths:
- Актуальная тема (Apple всегда в тренде)
- Четкая структура новости
- Хорошая читаемость

Weaknesses:
- Нет эмоционального хука
- Слабый призыв к действию
- Общая тема без конкретики

Recommendations:
1. Добавить конкретные цифры: "iPhone с батареей на 30% больше"
2. Усилить эмоции: "Это изменит всё" вместо "Представили новый"
3. Добавить CTA: "Что думаешь? Напиши в комментах"

Predicted Metrics:
- Retention: 45-55%
- Shares: 0.5-1%
- Viral Probability: medium
```

