# Исправление: Scout Agent - rssStorage.getRssItems is not a function

## 🐛 Проблема

При работе Scout Agent (конвейер контента) возникала ошибка:

```
Скаут: Ошибка - rssStorage.getRssItems is not a function
```

## 🔍 Причина

После рефакторинга архитектуры, метод `getRssItems` был перемещён из `rssStorage` в новый сервис `NewsService`, но Scout Agent продолжал использовать старый API:

**Проблемный код** (`scout-agent.ts:169`):
```typescript
import { rssStorage } from "../../storage/rss.storage";

// ...
const allItems = await rssStorage.getRssItems(userId); // ❌ Метода больше нет!
```

## ✅ Решение

Обновлён импорт и вызов метода для использования нового `NewsService`:

```typescript
import { newsService } from "../../modules/news/news.service";

// ...
const allItems = await newsService.getRssItems(userId); // ✅ Новый API
```

## 📝 Контекст архитектуры

### Старая архитектура (до рефакторинга):
```
storage/rss.storage.ts
  └─ getRssItems(userId) - прямой доступ к БД
```

### Новая архитектура (после рефакторинга):
```
modules/news/
  ├─ news.service.ts
  │   └─ getRssItems(userId) - бизнес-логика
  └─ news.repo.ts
      └─ getAll(userId) - доступ к БД
```

### Принцип:
- **Storage** - низкоуровневые операции с БД
- **Service** - бизнес-логика и публичные API
- **Controller** - обработка HTTP запросов

## 🎯 Результат

✅ Scout Agent использует актуальный API  
✅ Конвейер контента работает корректно  
✅ Методы вызываются через правильный слой архитектуры  

## 📂 Изменённые файлы

- `server/conveyor/agents/scout-agent.ts` - обновлён импорт и вызов

## 🔍 Аналогичные проблемы

Если в других местах кода встречаются вызовы несуществующих методов storage:
- `rssStorage.getRssItems()` → используйте `newsService.getRssItems()`
- `rssStorage.createRssItem()` → используйте `newsService` или `newsRepo`
- `rssStorage.updateRssItemAction()` → используйте `newsService` или `newsRepo`

## 📚 Связанные файлы

- `server/modules/news/news.service.ts` - новый API для работы с RSS
- `server/modules/news/news.repo.ts` - репозиторий для доступа к БД
- `server/storage/rss.storage.ts` - старый storage (частично deprecated)
