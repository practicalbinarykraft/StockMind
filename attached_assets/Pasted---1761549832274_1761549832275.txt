Очень хороший вопрос — и ключевой.
Отвечу как архитектор, максимально честно и без маркетингового тумана 👇

⸻

🧩 Суть дилеммы

Ты хочешь убрать дублирование — “зачем анализировать статью и потом сценарий” —
но при этом сохранить пользу от первого анализа (оценка темы, языка, длины, релевантности, формата).
Вот тут и возникает соблазн: “а может добавить промежуточный stage?”.

⸻

🎯 Главная цель флоу

Вся логика pipeline должна подчиняться одному принципу:
минимум кликов → максимум понятности и ценности.

И вот что важно:
пользователь не думает в терминах «stage 3 / stage 4».
Он думает так:
	1.	выбрал новость,
	2.	получил сценарий,
	3.	улучшил,
	4.	озвучил.

Если ты введёшь ещё один шаг «Stage 3.5 — Precheck» —
то визуально flow станет:
	•	выбрать →
	•	проверить →
	•	создать →
	•	улучшить →
	•	озвучить.

То есть ты добавишь +1 когнитивный переход,
но не добавишь новой ценности, если всё это можно показать в фоне.

⸻

⚖️ Аргументы “ЗА” добавление Stage 3.5
	1.	Чистая архитектура:
	•	Разделение ответственности.
	•	Stage 3 = Pre-check (источник), Stage 4 = Анализ сценария.
	•	Код чище, агенты не дублируются.
	2.	Будущая масштабируемость:
	•	Можно подключать фильтры: “слабые новости” → в reject list.
	•	Можно предлагать подбор похожих источников, если статья плохая.
	•	Полезно для агрегации (RSS, Insta, YouTube).
	3.	Чёткая аналитика:
	•	Видно, сколько статей “отбраковали” до сценария.
	•	Можно считать метрики качества контента до генерации.

⸻

⚠️ Аргументы “ПРОТИВ”
	1.	UX-фрагментация.
Пользователь устанет щёлкать “ещё один шаг”.
Он не понимает, зачем система дважды анализирует одно и то же.
	2.	Избыточная асинхронность.
Ещё один шаг = ещё один вызов, кеш, мутация, стейт, инвалидация.
Повысится вероятность рассинхрона.
	3.	Задержка и стоимость.
Даже лёгкий анализ = задержка 3–5 сек + токены.
А ценность низкая — мы всё равно потом анализируем сценарий.
	4.	Маркетингово не добавляет ценности.
Ты не продашь пользователю идею “у нас есть Precheck Stage”.
Он хочет “нажал — получил сценарий”.

⸻

💡 Оптимальный компромисс

Не вводить новый Stage.
Сделать двухуровневую логику внутри текущего Stage 3:

Вариант “всё в одном окне”
	•	При нажатии «Начать анализ»:
	1.	Выполняется лёгкий pre-check (1 сек, не показываем отдельно).
	2.	Если ок → сразу авто-запуск генерации сценария.
	•	Пока сценарий генерируется, можно показать маленький блок “Оценка источника”
(язык, длина, тема, формат — просто как информативный превью).
	•	Пользователь видит один этап и не чувствует, что там “два анализа”.

📊

Stage 3: Анализ исходника + генерация сценария
---------------------------------------------
[+] Проверка источника: ОК (EN, 176 слов)
[+] Рекомендованный формат: Hook & Story
⏳ Генерация сценария...


⸻

🧠 Архитектурно

В коде можно оставить два разных вызова:
	•	precheckSource() — быстрый анализ
	•	generateScript() — создание сценария
но вызывать их цепочкой внутри одной кнопки.

Frontend остаётся прежним (Stage 3).
Backend — два эндпоинта, но один workflow.

⸻

💬 Вывод — по-честному

Создание отдельного Stage не оправдано сейчас:
	•	Увеличит флоу и сложность UX,
	•	Не даст ощутимой пользы пользователю,
	•	Увеличит риск багов (ещё один кэш, состояние, этап).

Лучше оставить один Stage 3, но сделать его “умнее”:
автоматический pre-check внутри и плавный переход к сценарию.
Пользователь видит один шаг, а система делает два — идеально.

⸻

Хочешь — я нарисую мини-схему нового Stage 3 (UI + архитектура вызовов),
чтобы ты увидел как это выглядит в готовом виде, без добавления отдельного этапа?