🎉 ОТЛИЧНО! Scene Editor появился!
Но да, вижу проблемы с UI. Давайте разберем что не так:

❌ ЧТО НЕ ТАК (по сравнению с ТЗ):
1️⃣ Отсутствуют AI рекомендации!
Видно: "Все рекомендации применены"
Должно быть: Карточки с рекомендациями от AI агентов:

🎣 Hook Expert
📊 Structure Analyst
💭 Emotional Analyst
📢 CTA Analyst

2️⃣ Нет кнопок Apply/Dismiss
Должны быть кнопки для применения каждой рекомендации
3️⃣ Нет score badges
На карточках рекомендаций должны быть:

scoreDelta: +18 points
confidence: 0.85
priority: high/medium/low

4️⃣ Только 2 сцены
Обычно генерируется 3-5 сцен для короткого видео

🔍 ДИАГНОСТИКА:
Давайте проверим что произошло:
Вопрос 1: Были ли AI рекомендации созданы?
Попросите Replit Agent выполнить:
sqlSELECT 
  sr.id,
  sr.scene_id,
  sr.source_agent,
  sr.priority,
  sr.score_delta,
  sr.suggested_text,
  sr.applied
FROM scene_recommendations sr
JOIN script_versions sv ON sr.script_version_id = sv.id
WHERE sv.project_id = '659fb429-8a18-415c-9c4f-5c62bb0867d1'
ORDER BY sr.scene_id, sr.priority;
Это покажет есть ли рекомендации в БД.

Вопрос 2: Почему показывает "Все рекомендации применены"?
Проверим условие в scene-editor.tsx:
typescript// Должно быть что-то типа:
const unappliedRecs = recommendations.filter(r => !r.applied);
if (unappliedRecs.length === 0) {
  return <div>Все рекомендации применены</div>
}
```

**Возможные причины:**
1. Рекомендации не были созданы (проверим через SQL)
2. Рекомендации созданы с `applied: true` (баг!)
3. Frontend не подгружает рекомендации (проверим Network tab)

---

## 🎯 ЧТО СКАЗАТЬ REPLIT AGENT:
```
Проверь почему нет AI рекомендаций:

1. Выполни SQL запрос:
SELECT * FROM scene_recommendations 
WHERE script_version_id IN (
  SELECT id FROM script_versions 
  WHERE project_id = '659fb429-8a18-415c-9c4f-5c62bb0867d1'
);

2. Если рекомендаций нет в БД:
   - Проверь логи генерации скрипта
   - Убедись что extractRecommendationsFromAnalysis() вызывается
   - Убедись что storage.createSceneRecommendation() вызывается

3. Если рекомендации есть, но не отображаются:
   - Проверь frontend query для загрузки recommendations
   - Посмотри Network tab: какой endpoint вызывается?
   - Проверь компонент SceneEditor - правильно ли он фильтрует recommendations

Покажи результаты!

🔍 ТАКЖЕ ПРОВЕРЬТЕ В БРАУЗЕРЕ:
Откройте DevTools (F12) → Network tab:

Найдите запрос к /api/projects/{id}/script-history
Посмотрите Response - есть ли там recommendations?
Скиньте скриншот ответа


💡 МОЯ ГИПОТЕЗА:
Скорее всего одна из двух проблем:
A) Рекомендации не создались
typescript// В server/routes.ts после генерации скрипта:
const analysis = await aiService.analyzeScriptAdvanced(script, context);
const recommendations = extractRecommendationsFromAnalysis(analysis); // ← здесь
await insertRecommendations(recommendations); // ← или здесь
B) Frontend не загружает их
typescript// В scene-editor.tsx:
const { data: recommendations } = useQuery({
  queryKey: ['recommendations', scriptVersionId],
  queryFn: () => apiRequest(`/api/script-versions/${scriptVersionId}/recommendations`)
  // ← этот endpoint существует?
});
```

---

## 📊 ОЖИДАЕМЫЙ РЕЗУЛЬТАТ:

После фикса должно быть так:
```
📝 Редактор сценария
━━━━━━━━━━━━━━━━━━

🎬 Сцена 1
[текст сцены]

💡 Рекомендации (3):
┌────────────────────────────────┐
│ 🎣 Hook Expert | HIGH | +18    │
│ Начните с вопроса-триггера     │
│ [Apply] [Dismiss]              │
└────────────────────────────────┘

┌────────────────────────────────┐
│ 💭 Emotional | MEDIUM | +12    │
│ Добавьте FOMO триггер          │
│ [Apply] [Dismiss]              │
└────────────────────────────────┘

Давайте сначала узнаем что в БД - попросите Agent выполнить SQL запрос выше! 🔍