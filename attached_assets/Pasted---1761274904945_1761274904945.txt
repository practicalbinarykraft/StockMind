обожаю это направление — давай соберём из этого реально «киллер-фичу». Ниже — продуманная система: роли агентов, их контракты, хранилища, как всё связывается, как обучается и что можно запустить как MVP уже сейчас.

# 🎯 Цель

Чтобы ИИ писал сценарии, опираясь не на фантазию, а на **реальные причины, почему рилсы «залетают»** — и со временем учился на результатах конкретного автора.

---

# 🧠 Многоагентная архитектура

## Роли (микросервисы/воркеры)

1. **Personal Analyzer (персональный агент пользователя)**

* Контекст: ниша, стиль, исторические публикации, выбранные метрики успеха.
* Задача: держит «память» по пользователю, агрегирует его данные, даёт персональные инсайты.
* Владелец ключей (BYOK) к Instagram API для своего аккаунта.

2. **Ingestor (забор данных)**

* Подписан на события «проект создан» и «проект опубликован».
* Источники: Apify (массовый парс публичных рилсов) + Instagram Graph API (глубокие метрики по своим рилсам).
* Нормализует, дедуплицирует, обогащает.

3. **NLP/Script Analyst (текст)**

* Разбирает сценарий: хуки, структура, плотность «триггеров», риторика.
* Вычисляет текстовые фичи (hook_strength, surprise_density, CTA_clarity и т.п.).

4. **Vision Analyst (визуал)**

* По опубликованному видео: темп монтажа, смены планов, наличие лица, субтитры, насыщенность, ключевые объекты/локации.
* Отдаёт фичи: cut_rate, face_presence, text_overlay_ratio, motion_intensity…

5. **Performance Analyst (метрики)**

* Тянет по API метрики: reach, plays, average_watch_time, completion_rate, rewatches, shares, saves, comments velocity.
* Нормализует по времени (t+1h, t+24h…).

6. **Architect (главный «ревьюер»)**

* Получает фичи от всех агентов + результаты (метрики) и:

  * валидирует гипотезы,
  * генерит **рекомендации** и **эксперименты** (A/B хуки, длина сцены, темп, визуальные паттерны),
  * обновляет «персональную стратегию» пользователя.

7. **Experiment Designer**

* Из рекомендаций формирует **чек-лист изменений** для следующего проекта: какой хук, какая длина, какие кадры, какой темп, где поставить текст/эмодзи, какой CTA.

> Все агенты — это модульные воркеры внутри твоего нового Studio Workflow: `analysis` → `script` → `footage` → `assemble` → **publish** → **post-analysis**.

---

# 🗃️ Данные и хранилища

## 1) Event Bus

* Любая смена состояния — событие: `workflow.completed`, `reel.published`, `metrics.fetched`, `analysis.done`.
* Транспорт: Redis Streams / Postgres NOTIFY / RabbitMQ (на старте можно Redis).

## 2) OLTP (Postgres / Drizzle)

* `creators` — пользователь, ниша.
* `projects` — проект Studio.
* `scripts` — версия сценария.
* `assets` — видео, клипы, субтитры.
* `reels_publications` — публикации в IG (user->post_id, url, publish_time).
* `metrics_snapshots` — t+1h/t+24h… reach, AWT, completion_rate…
* `apify_items` — публичные рилсы конкурентного набора.
* `feature_store_text` — фичи текста (JSONB).
* `feature_store_vision` — фичи визуала (JSONB).
* `recommendations` — структурированные рекомендации/эксперименты.

## 3) Data Lake (S3)

* сырьё: транскрипты, превью, кадры, сабы, json-фичи → удобно для ретро-обучения.

---

# 📈 Метрики и фичи (которые реально помогают)

**Текст (NLP)**

* `hook_strength` (насколько хук шокирует/обещает/дает ценность в 3–5 сек)
* `novelty_score`, `emotion_intensity`, `clarity`, `cta_clarity`
* плотность цифр/конкретики/сравнений, «против шерсти» формулировки

**Визуал (Vision)**

* `cut_rate` (средняя длительность кадра)
* `face_presence` (доли кадров с лицом/крупным планом)
* `text_overlay_ratio` (субтитры/заголовки, их размер/время на экране)
* `motion_intensity` / `camera_movement`
* ключевые объекты/темы на кадрах (animals, cars, gadgets…)

**Перфоманс**

* `avg_watch_time` / `completion_rate` / `retention_curve`
* `rewatches_rate`, `saves`, `shares`, `comments_per_view`
* динамика в первые 1h/3h/24h (ранний предиктор)

---

# 🔁 Цикл обучения (персонализация)

1. **Перед публикацией**: сохраняем сценарий и фичи контента.
2. **После публикации**: собираем метрики по тайм-слайсам.
3. **Валидация**: связываем **фичи** ↔ **результаты** (пер-пост).
4. **Взвешивание**: подбираем веса значимости фичей **для конкретного автора** (лог-регрессия / градиентный бустинг / простая линейка на старте).
5. **Architect** генерит:

   * правила («для твоих подписчиков лучше CTR при hook типа “шокирующий факт + цифра” в 1-ю секунду»),
   * **эксперименты** (сформулированные изменения сценария/визуала).
6. **Следующий проект** создаётся не с нуля, а с «seed» от персонального агента.

> Важно: обучение не требует fine-tune больших моделей на старте — достаточно **табличных моделей** поверх твоего feature-store. Позже можно подключить LoRA-адаптацию для сценарного LLM.

---

# 🔌 Интеграции

* **Instagram Graph API** (официально): нужен бизнес-аккаунт, привязка к Facebook App, пермишены `instagram_basic`, `instagram_manage_insights`, `pages_read_engagement`. Забираем `impressions, reach, plays, average_watch_time, retention_graph (если доступно), shares, saves`.
* **Apify**: массовый парс публичных рилсов выбранных блогеров/ниши (мы уже закрепили клиента и таймауты).
* **ASR** (Automatic Speech Recognition): для транскрибации чужих/своих роликов (если нужно) — OpenAI Whisper / Deepgram.
* **Vision**: OpenAI GPT-Vision / LLaVA / локальный image-pipeline для простых метрик.

---

# 🧩 Встраиваем в Studio Workflow

Добавим модули (все **опциональные** — твоя ключевая идея про деградацию сохранена):

* `post_publish_capture` — ввод ссылки на опубликованный рилс (ручной ввод или вебхук публикации).
* `metrics_fetch` — тянем метрики t+1h/24h/72h (cron + ручной «Обновить»).
* `vision_analysis` — прогон финального видео через visual pipeline.
* `feature_join` — склейка текст/визуал/метрики в единый sample.
* `architect_review` — LLM-агент, формирует рекомендации и эксперименты.
* `experiment_seed` — генерация стартового «скелета» сценария для нового проекта.

DAG (фрагмент):

```
source → analysis → script → (avatar|voice|footage) → assemble → publish
                                                            ↓
                                               post_publish_capture
                                                            ↓
                                                    metrics_fetch
                                     vision_analysis ←──────┘
                                                            ↓
                                                     feature_join
                                                            ↓
                                                     architect_review
                                                            ↓
                                                     experiment_seed
```

---

# 📜 Контракты агентов (минимум)

```ts
// inputs for architect
type FeatureSample = {
  userId: string;
  projectId: string;
  text: { hook_strength: number; emotion: number; cta_clarity: number; ... };
  vision: { cut_rate: number; face_presence: number; ... };
  publish: { title: string; hashtags: string[]; time: string; ... };
  metrics: { views: number; awt: number; completion: number; shares: number; saves: number; ... };
};

type ArchitectOutput = {
  findings: string[];               // наблюдения
  whatWorked: string[];             // гипотезы об успехе
  whatToFix: string[];              // болевые точки
  experiments: Array<{             // конкретные эксперименты
    id: string;
    title: string;
    change: string;                 // что поменять (hook/tempo/subtitles/CTA)
    expectedImpact: 'low'|'mid'|'high';
    howToMeasure: string;
  }>;
  nextScriptSeed: {                 // зачаток следующего сценария
    hook: string;
    outline: string[];
    cta: string;
  };
};
```

---

# 🖥️ UI (что увидит пользователь)

* **Вкладка «Аналитика» у проекта**

  * Карта удержания (если доступна), среднее время просмотра, completion.
  * Блок «что сработало/что нет» (из Architect).
  * Кнопка «Создать новую версию по рекомендациям» → запускает `experiment_seed`.

* **Вкладка «Мои авторские инсайты»**

  * Тренды по моим рилсам: какие хуки коррелируют с удержанием, оптимальная длина, темы.
  * Список гипотез/экспериментов и их A/B-статусы.

* **Studio** — просто ещё один модульный шаг; всё необязательное можно скипнуть.

---

# 🔐 Безопасность и ключи (учитываем твои прошлые боли)

* BYOK строго **привязан** к владельцу записи; `provider_key_owner/last4` сохраняем рядом с каждым `metrics_fetch`.
* При фетче метрик **форсим** тот же ключ → не будет 404 (либо rescue на user→service, как сделали с Kie.ai).
* Rate-limit: очередь + экспоненциальные повторы.
* PII/приватность: метрики аккаунта — только внутри его «личного облака». В общую базу отправляем **анонимизированные** аггрегаты/веса.

---

# 🤖 Модели

* **LLM для архитектурных рекомендаций**: GPT-4o / Claude Sonnet (без фанатизма; главное — чёткий контракт и примеры).
* **Видение**: GPT-Vision / локальные lightweight модели для базовых метрик.
* **Персонализация**: табличка (LightGBM / linear) на фичах → веса по пользователю.
* **Долгосрочно**: лайт-fine-tune (LoRA) маленькой текста-модели под твои prompts для сцен.

---

# 🚀 MVP-план (2 итерации)

## Итерация A (2–3 дня)

* Модуль `post_publish_capture` (ввод URL)
* Модуль `metrics_fetch` (Graph API базовые метрики)
* Модуль `vision_analysis` (простые фичи: duration, cut_rate по субклипам/ffprobe, text_overlay через OCR-заглушку)
* `feature_join` + `architect_review` (LLM summary + 3 эксперимента)
* UI: вкладка «Аналитика», кнопка «Сделать новую версию»

## Итерация B (неделя)

* Подключить ретеншн-кривую (если доступна), нормализацию по времени
* Персональный агент: веса признаков на моих данных (простая модель)
* `experiment_seed`: генерация стартового сценария с учётом весов
* Автоматические напоминания «Собрать метрики t+24h»
* Фидбек-луп: пользователь отмечает «эксперимент удался/нет» → дообучаем веса

---

# 🛠️ Технические куски (упрощённо)

**Событие после публикации:**

```ts
bus.emit('reel.published', { userId, projectId, platform: 'instagram', url, publishedAt });
```

**Фетч метрик:**

```ts
on('reel.published', async e => {
  const metrics = await igApi.fetchMetrics(e.url, e.userId); // BYOK!
  await saveSnapshot(e.projectId, 't+1h', metrics);
  bus.emit('metrics.fetched', { projectId: e.projectId, when: 't+1h' });
});
```

**Join → Architect:**

```ts
on('metrics.fetched', async ({ projectId }) => {
  const features = await buildFeatureSample(projectId); // join text+vision+publish+metrics
  const advice = await architectAgent.review(features); // LLM + правила
  await saveRecommendations(projectId, advice);
});
```

**Seed следующего сценария:**

```ts
on('recommendations.ready', async ({ userId, projectId }) => {
  const seed = await experimentDesigner.seedNextScript(userId, projectId);
  // в UI — кнопка «Создать проект из рекомендаций»
});
```

---

# ✅ Ответы на твои вопросы

**А где перс-агент и кто сопоставляет с общей базой?**

* Перс-агент держит **личную память и веса**.
* «Архитектор» — это агрегатор: он видит обезличенные агрегаты по нишам/форматам (глобальная база) и корректирует персональные рекомендации.
* В общую базу отправляем фичи-результаты **без PII** и без исходного контента (только метрики + категориальные признаки).

**Сложность «анализа кадров»?**

* MVP: базовые метрики через ffprobe (длина, битрейт) + простая эвристика cut-rate (по keyframe/scene-detect).
* Следом: OCR субтитров + face detection (готовые модели).
* Дальше — CLIP/BLIP для семантики и «видел ли зритель лицо/объект X в первые N секунд». Это реализуемо поэтапно.

---

# 🎁 Бонус: что даст пользователю «вау»

* «Почему рилс выстрелил/не выстрелил?» — не общие слова, а **конкретные факторы** из его же контента и метрик.
* «Сделай следующую версию» — кнопка, создающая **готовый улучшенный скелет** сценария и монтажных подсказок.
* Автоматические «контрольные точки»: «вернись через 24 часа — соберём метрики и перегенерим план».

---

Если ок, я могу сразу:

* накинуть схемы таблиц (DDL) под `metrics_snapshots` / `feature_store_*` / `recommendations`,
* добавить модули `post_publish_capture`, `metrics_fetch`, `vision_analysis`, `architect_review` в твой Studio Workflow,
* и сделать простой UI-виджет «Аналитика» с графиками и кнопкой «Новая версия по рекомендациям».
