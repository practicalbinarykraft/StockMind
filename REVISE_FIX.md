# 🔧 Исправление ошибки 500 при отправке на доработку (revise)

## ❌ Проблема

При отправке сценария на доработку через `POST /api/auto-scripts/:id/revise` возникала ошибка 500 (Internal Server Error).

## 🔍 Найденные причины

### 1. **Неправильный импорт функции расшифровки** (КРИТИЧНО)

**Файл:** `server/conveyor/learning-service.ts:172`

**Проблема:** Использовался неправильный импорт:
```typescript
const { decrypt } = await import("../storage/base/encryption");
```

**Решение:** Исправлено на:
```typescript
const { decryptApiKey } = await import("../storage/base/encryption");
```

Функция `decrypt` не существует в модуле `encryption.ts`, там экспортируется только `decryptApiKey`.

---

### 2. **Отсутствие проверки существования скрипта**

**Файл:** `server/conveyor/learning-service.ts:150-162`

**Проблема:** Если скрипт не найден, код пытался обратиться к `script?.fullScript`, что могло вызвать проблемы.

**Решение:** Добавлена проверка:
```typescript
if (!script) {
  logger.warn("[Learning] Script not found for revision", { scriptId });
  return;
}
```

---

### 3. **Недостаточное логирование ошибок**

**Проблема:** При ошибке в `learningService.onRevise()` не было детального логирования, что затрудняло отладку.

**Решение:** 
- Добавлен try-catch в роуте `auto-scripts.routes.ts`
- Добавлено детальное логирование в `learning-service.ts`
- Ошибка в learning service больше не ломает весь запрос (revision всё равно помечается)

---

## ✅ Исправления

### Изменённые файлы:

1. **`server/conveyor/learning-service.ts`**
   - ✅ Исправлен импорт `decrypt` → `decryptApiKey`
   - ✅ Добавлена проверка существования скрипта
   - ✅ Улучшено логирование ошибок

2. **`server/routes/auto-scripts.routes.ts`**
   - ✅ Добавлен try-catch для `learningService.onRevise()`
   - ✅ Ошибка в learning service не ломает запрос revision

---

## 🧪 Как проверить

1. Откройте сценарий в статусе "pending"
2. Нажмите "Отправить на доработку"
3. Введите текст рецензии
4. Отправьте форму

**Ожидаемый результат:**
- ✅ Запрос успешно обрабатывается (200 OK)
- ✅ Сценарий помечается как "revision"
- ✅ Feedback entry создаётся в БД
- ✅ Learning service обрабатывает feedback (если есть API ключ)

**Если всё ещё ошибка:**
- Проверьте логи сервера - теперь там будет детальная информация об ошибке
- Убедитесь, что есть API ключ Anthropic (для AI обработки feedback)
- Проверьте, что скрипт существует в БД

---

## 📝 Дополнительные улучшения

- Добавлена обработка ошибок в learning service без падения всего запроса
- Улучшено логирование для отладки
- Добавлена проверка существования скрипта перед обработкой

---

**Статус:** ✅ Исправлено и готово к тестированию

