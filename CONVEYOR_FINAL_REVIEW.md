# ✅ Финальный Review: Обновления к плану

## 📊 Анализ изменений

### ✅ Что хорошо:

1. **Единая модель (Sonnet)** - проще, предсказуемо, качество выше ✅
2. **Настраиваемый daily_limit** - гибкость для пользователя ✅
3. **Прозрачная стоимость** - пользователь видит ~$0.08 за сценарий ✅

---

## ⚠️ Критические замечания:

### 1. **QC Agent - 4 parallel Sonnet calls = $0.024**

**Проблема:** Это 30% от общей стоимости ($0.024 из $0.08)!

**Сравнение:**
- Вариант 1 (план): 4 parallel Sonnet = $0.024
- Вариант 2 (мой): 1 call через `analyzeScriptAdvanced()` = ~$0.015-0.020

**Рекомендация:** Использовать `analyzeScriptAdvanced()` - экономия $0.004-0.009 на item (5-11% экономии).

**Но:** Если нужна максимальная точность QC → можно оставить 4 calls.

---

### 2. **Стоимость выросла: $0.06 → $0.08**

**Влияние:**
- 10 items/день = $0.80/день = $24/месяц (было $18)
- 50 items/день (макс) = $4/день = $120/месяц

**Вопрос:** Нужен ли budget limit на пользователя?

**Предложение:**
```typescript
interface ConveyorSettings {
  dailyLimit: number; // 1-50, настраивается
  monthlyBudgetLimit?: number; // опционально, например $50
  currentMonthCost: number;
  
  // При превышении monthlyBudgetLimit:
  // - Остановить обработку
  // - Или переключить на более дешёвые модели?
}
```

---

### 3. **Daily limit 1-50 - валидация**

**Вопрос:** Нужна ли валидация разумности?

**Примеры:**
- Пользователь ставит 50 → $4/день = $120/месяц
- Нужно ли предупреждение при высоком лимите?
- Нужен ли hard limit (например, макс 30)?

**Предложение:**
```typescript
// В UI показать предупреждение
if (dailyLimit > 30) {
  showWarning(
    `Высокий лимит: ${dailyLimit} сценариев/день = ~$${(dailyLimit * 0.08 * 30).toFixed(0)}/месяц`
  );
}
```

---

## ✅ Что осталось уточнить:

### Критично (перед началом):

1. **QC Agent** - 4 calls или 1 call через `analyzeScriptAdvanced()`?
   - 4 calls = точнее, но дороже ($0.024)
   - 1 call = дешевле ($0.015-0.020), использует существующий код

2. **Revision Flow** - как связывать revisions с auto_script?
   - Обновлять существующий `auto_script`?
   - Или создавать новый с `parent_script_id`?

3. **Checkpointing** - нужен ли для retry?
   - При retry начинать с начала или с последнего успешного stage?

### Желательно:

4. **Budget limits** - нужны ли ограничения по стоимости?
   - Monthly budget limit?
   - Hard limit на daily_limit?

5. **Manual trigger limits** - применяется ли daily_limit к ручному запуску?

---

## 💡 Дополнительные предложения:

### 1. **Показывать стоимость в UI**

```tsx
// В Settings показывать:
<div>
  <Slider value={dailyLimit} min={1} max={50} />
  <p>
    ~${(dailyLimit * 0.08).toFixed(2)}/день 
    (~${(dailyLimit * 0.08 * 30).toFixed(0)}/месяц)
  </p>
</div>
```

### 2. **Предупреждение при высоком лимите**

```typescript
if (dailyLimit > 30) {
  return {
    warning: `Высокий лимит: ~$${(dailyLimit * 0.08 * 30).toFixed(0)}/месяц`,
    suggestion: 'Рекомендуем начать с 10-15 сценариев/день'
  };
}
```

### 3. **Статистика по стоимости**

```typescript
interface ConveyorStats {
  itemsProcessed: number;
  totalCost: number;
  averageCostPerItem: number;
  currentMonthCost: number;
  projectedMonthCost: number; // на основе текущего daily_limit
}
```

---

## 🎯 Итоговая оценка:

**Оценка: 9/10** - Отличный план, осталось уточнить 3 критичных вопроса.

### Готов начать после уточнения:

1. ✅ QC Agent - 4 calls или 1 call?
2. ✅ Revision Flow - как связывать?
3. ✅ Checkpointing - нужен ли?

### Остальное можно решить в процессе:

4. ⚠️ Budget limits (можно добавить позже)
5. ⚠️ Manual trigger limits (можно добавить позже)

---

## 📝 Рекомендации по реализации:

### Приоритет 1 (сделать сразу):

1. ✅ Использовать единую модель Sonnet (как в плане)
2. ✅ Настраиваемый daily_limit 1-50
3. ✅ Показывать стоимость в UI

### Приоритет 2 (можно оптимизировать):

4. ⚠️ QC Agent - рассмотреть использование `analyzeScriptAdvanced()` для экономии
5. ⚠️ Добавить предупреждение при daily_limit > 30

### Приоритет 3 (можно отложить):

6. 💡 Budget limits (если нужно)
7. 💡 Статистика по стоимости (если нужно)

---

**Готов начать реализацию после уточнения 3 критичных вопросов!** 🚀

