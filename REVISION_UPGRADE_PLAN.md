# 📋 ПЛАН АПГРЕЙДА РЕВИЗИИ - JUNIOR-FRIENDLY CODE

**Принципы:**
- ✅ Модульная архитектура - каждый файл <200 строк
- ✅ Один файл = одна ответственность
- ✅ Понятные имена функций и переменных
- ✅ Никаких монстр-файлов

---

## 🎯 ЦЕЛИ АПГРЕЙДА

1. **Индикация прогресса ревизии** - показывать этапы обработки
2. **Сравнение версий** - diff между версиями, изменения scores
3. **Логи обработки** - детали каждого этапа
4. **Улучшение UX** - понятные сообщения, неблокирующий UI

---

## 📁 СТРУКТУРА ФАЙЛОВ (ПОСЛЕ РЕФАКТОРИНГА)

```
client/src/pages/auto-scripts/
├── index.tsx (150 строк) - главная страница, только композиция
│
├── components/
│   ├── ScriptList.tsx (120 строк) - список сценариев
│   ├── ScriptPreview.tsx (180 строк) - превью выбранного сценария
│   ├── RevisionProgress.tsx (150 строк) - индикатор прогресса ревизии
│   ├── RevisionStatus.tsx (100 строк) - статус ревизии с сообщениями
│   ├── VersionComparison.tsx (180 строк) - сравнение версий
│   └── RevisionLogs.tsx (150 строк) - логи обработки
│
├── hooks/
│   ├── use-auto-scripts.ts (120 строк) - загрузка списка сценариев
│   ├── use-revision-progress.ts (150 строк) - отслеживание прогресса ревизии
│   ├── use-revision-logs.ts (100 строк) - загрузка логов обработки
│   └── use-version-comparison.ts (120 строк) - сравнение версий
│
└── utils/
    ├── revision-stages.ts (80 строк) - константы этапов обработки
    └── version-diff.ts (150 строк) - логика сравнения версий
```

---

## 📝 ПОШАГОВЫЙ ПЛАН

### ЭТАП 1: Подготовка инфраструктуры (День 1)

#### Шаг 1.1: Создать утилиты для этапов обработки
**Файл:** `client/src/pages/auto-scripts/utils/revision-stages.ts`

**Что делает:**
- Константы этапов обработки (Writer, QC, Optimizer, Gate)
- Функции для получения названий этапов
- Функции для получения иконок этапов

**Размер:** ~80 строк

**Зависимости:** Нет

---

#### Шаг 1.2: Создать API endpoint для прогресса ревизии
**Файл:** `server/routes/conveyor-progress.routes.ts` (новый)

**Что делает:**
- `GET /api/conveyor/items/:id/progress` - получить прогресс обработки
- `GET /api/conveyor/items/:id/logs` - получить логи обработки

**Размер:** ~150 строк

**Зависимости:** Нет

---

### ЭТАП 2: Хуки для работы с ревизией (День 2)

#### Шаг 2.1: Хук для отслеживания прогресса ревизии
**Файл:** `client/src/pages/auto-scripts/hooks/use-revision-progress.ts`

**Что делает:**
- Загружает прогресс обработки из API
- Обновляет каждые 2 секунды (если статус "processing")
- Возвращает: текущий этап, время обработки, прогресс (%)

**Размер:** ~150 строк

**Зависимости:**
- `utils/revision-stages.ts`

**Пример использования:**
```typescript
const { currentStage, progress, elapsedTime } = useRevisionProgress(conveyorItemId);
```

---

#### Шаг 2.2: Хук для загрузки логов обработки
**Файл:** `client/src/pages/auto-scripts/hooks/use-revision-logs.ts`

**Что делает:**
- Загружает логи обработки из API
- Фильтрует логи по этапам
- Возвращает: массив логов с временем, этапом, сообщением

**Размер:** ~100 строк

**Зависимости:** Нет

**Пример использования:**
```typescript
const { logs, isLoading } = useRevisionLogs(conveyorItemId);
```

---

#### Шаг 2.3: Хук для сравнения версий
**Файл:** `client/src/pages/auto-scripts/hooks/use-version-comparison.ts`

**Что делает:**
- Загружает версии сценария из API
- Вычисляет diff между версиями
- Вычисляет изменения scores

**Размер:** ~120 строк

**Зависимости:**
- `utils/version-diff.ts`

**Пример использования:**
```typescript
const { comparison, diff } = useVersionComparison(scriptId, version1, version2);
```

---

### ЭТАП 3: Компоненты для отображения (День 3-4)

#### Шаг 3.1: Компонент индикатора прогресса ревизии
**Файл:** `client/src/pages/auto-scripts/components/RevisionProgress.tsx`

**Что делает:**
- Показывает текущий этап обработки
- Показывает прогресс-бар
- Показывает время обработки
- Показывает примерное время до завершения

**Размер:** ~150 строк

**Зависимости:**
- `hooks/use-revision-progress.ts`
- `utils/revision-stages.ts`

**UI:**
```
┌─────────────────────────────────────┐
│ Ревизия в процессе                  │
│                                     │
│ [████████░░] 80%                    │
│                                     │
│ Этап: QC (проверка качества)       │
│ Время: 1 мин 23 сек                 │
│ Осталось: ~30 сек                   │
└─────────────────────────────────────┘
```

---

#### Шаг 3.2: Компонент статуса ревизии
**Файл:** `client/src/pages/auto-scripts/components/RevisionStatus.tsx`

**Что делает:**
- Показывает статус ревизии (в процессе / завершена / ошибка)
- Показывает сообщения для пользователя
- Показывает кнопку "Сбросить" (если нужно)

**Размер:** ~100 строк

**Зависимости:** Нет

**UI:**
```
┌─────────────────────────────────────┐
│ ⏳ Ревизия в процессе                │
│ Обычно занимает 2-4 минуты           │
│                                     │
│ [Сбросить]                          │
└─────────────────────────────────────┘
```

---

#### Шаг 3.3: Компонент логов обработки
**Файл:** `client/src/pages/auto-scripts/components/RevisionLogs.tsx`

**Что делает:**
- Показывает логи обработки по этапам
- Показывает время каждого этапа
- Показывает ошибки (если есть)

**Размер:** ~150 строк

**Зависимости:**
- `hooks/use-revision-logs.ts`

**UI:**
```
┌─────────────────────────────────────┐
│ Логи обработки                      │
│                                     │
│ ✅ Writer: Завершён (15 сек)        │
│ ✅ QC: Завершён (10 сек)            │
│ ⏳ Optimizer: В процессе...         │
│ ⏸ Gate: Ожидание                   │
│                                     │
│ [Скрыть логи]                       │
└─────────────────────────────────────┘
```

---

#### Шаг 3.4: Компонент сравнения версий
**Файл:** `client/src/pages/auto-scripts/components/VersionComparison.tsx`

**Что делает:**
- Показывает diff между версиями
- Показывает изменения scores
- Показывает, что улучшилось/ухудшилось

**Размер:** ~180 строк

**Зависимости:**
- `hooks/use-version-comparison.ts`
- `utils/version-diff.ts`

**UI:**
```
┌─────────────────────────────────────┐
│ Сравнение версий                    │
│                                     │
│ Версия 1 (оригинал)                 │
│ Score: 72/100                       │
│                                     │
│ Версия 2 (после ревизии)            │
│ Score: 83/100 ⬆️ +11                │
│                                     │
│ Изменения:                          │
│ • Хук: 75 → 83 (+8) ✅             │
│ • Структура: 70 → 82 (+12) ✅       │
│ • Убраны клише "СТОП!"              │
│                                     │
│ [Показать diff текста]              │
└─────────────────────────────────────┘
```

---

### ЭТАП 4: Рефакторинг главной страницы (День 5)

#### Шаг 4.1: Разбить index.tsx на компоненты
**Файл:** `client/src/pages/auto-scripts/index.tsx`

**Что делает:**
- Только композиция компонентов
- Управление состоянием (выбранный сценарий)
- Обработка действий (одобрить, отклонить, доработать)

**Размер:** ~150 строк (было 635)

**Зависимости:**
- Все компоненты из `components/`
- Все хуки из `hooks/`

**Структура:**
```typescript
export default function AutoScriptsPage() {
  const [selectedScript, setSelectedScript] = useState<AutoScript | null>(null);
  
  return (
    <div>
      <ScriptList 
        onSelect={setSelectedScript}
        selectedId={selectedScript?.id}
      />
      {selectedScript && (
        <ScriptPreview 
          script={selectedScript}
          onApprove={...}
          onReject={...}
          onRevise={...}
        />
      )}
    </div>
  );
}
```

---

#### Шаг 4.2: Компонент списка сценариев
**Файл:** `client/src/pages/auto-scripts/components/ScriptList.tsx`

**Что делает:**
- Показывает список сценариев
- Обрабатывает выбор сценария
- Показывает бейджи (На ревизии, Ревизия #N)

**Размер:** ~120 строк

**Зависимости:**
- `hooks/use-auto-scripts.ts`

---

#### Шаг 4.3: Компонент превью сценария
**Файл:** `client/src/pages/auto-scripts/components/ScriptPreview.tsx`

**Что делает:**
- Показывает детали выбранного сценария
- Показывает scores
- Показывает сцены
- Показывает кнопки действий
- Интегрирует компоненты ревизии

**Размер:** ~180 строк

**Зависимости:**
- `components/RevisionProgress.tsx`
- `components/RevisionStatus.tsx`
- `components/RevisionLogs.tsx`
- `components/VersionComparison.tsx`

---

### ЭТАП 5: Утилиты для сравнения версий (День 6)

#### Шаг 5.1: Утилита для вычисления diff
**Файл:** `client/src/pages/auto-scripts/utils/version-diff.ts`

**Что делает:**
- Вычисляет diff между двумя версиями сценария
- Вычисляет изменения scores
- Вычисляет изменения в сценах

**Размер:** ~150 строк

**Зависимости:**
- Библиотека `diff` (npm install diff)

**Функции:**
```typescript
export function calculateScoreDiff(version1, version2): ScoreDiff
export function calculateSceneDiff(version1, version2): SceneDiff[]
export function calculateTextDiff(text1, text2): DiffResult
```

---

### ЭТАП 6: Backend API для прогресса (День 7)

#### Шаг 6.1: API endpoint для прогресса
**Файл:** `server/routes/conveyor-progress.routes.ts` (новый)

**Что делает:**
- `GET /api/conveyor/items/:id/progress` - получить прогресс обработки
- `GET /api/conveyor/items/:id/logs` - получить логи обработки

**Размер:** ~150 строк

**Зависимости:**
- `storage/conveyor-items.storage.ts`
- `storage/conveyor-logs.storage.ts`

**Пример ответа:**
```json
{
  "currentStage": 6,
  "stageName": "QC",
  "progress": 75,
  "elapsedTime": 83,
  "estimatedTimeRemaining": 30,
  "stages": [
    { "stage": 5, "name": "Writer", "status": "completed", "duration": 15 },
    { "stage": 6, "name": "QC", "status": "processing", "duration": 10 }
  ]
}
```

---

## 📊 ПРИОРИТЕТЫ ВЫПОЛНЕНИЯ

### Критично (сделать сначала):

1. **Шаг 1.1** - Утилиты для этапов (30 мин)
2. **Шаг 2.1** - Хук для прогресса (2 часа)
3. **Шаг 3.1** - Компонент прогресса (2 часа)
4. **Шаг 6.1** - Backend API (2 часа)

**Итого:** ~7 часов

---

### Важно (сделать потом):

5. **Шаг 3.2** - Компонент статуса (1 час)
6. **Шаг 3.3** - Компонент логов (2 часа)
7. **Шаг 2.2** - Хук для логов (1 час)

**Итого:** ~4 часа

---

### Желательно (можно позже):

8. **Шаг 3.4** - Компонент сравнения (3 часа)
9. **Шаг 2.3** - Хук для сравнения (2 часа)
10. **Шаг 5.1** - Утилита diff (2 часа)
11. **Шаг 4.1-4.3** - Рефакторинг главной страницы (4 часа)

**Итого:** ~11 часов

---

## ✅ КРИТЕРИИ УСПЕХА

### После завершения критичных задач:

- ✅ Пользователь видит прогресс ревизии (этап, время, %)
- ✅ Пользователь понимает, что происходит
- ✅ Пользователь может работать с другими сценариями
- ✅ Пользователь видит, когда ревизия завершена

### После завершения важных задач:

- ✅ Пользователь видит логи обработки
- ✅ Пользователь видит детали каждого этапа
- ✅ Пользователь видит ошибки (если есть)

### После завершения желательных задач:

- ✅ Пользователь может сравнить версии
- ✅ Пользователь видит diff между версиями
- ✅ Пользователь видит изменения scores
- ✅ Код разбит на модули <200 строк

---

## 📝 ЧЕКЛИСТ ВЫПОЛНЕНИЯ

### ЭТАП 1: Подготовка
- [ ] Шаг 1.1: Создать `utils/revision-stages.ts`
- [ ] Шаг 1.2: Создать `server/routes/conveyor-progress.routes.ts`

### ЭТАП 2: Хуки
- [ ] Шаг 2.1: Создать `hooks/use-revision-progress.ts`
- [ ] Шаг 2.2: Создать `hooks/use-revision-logs.ts`
- [ ] Шаг 2.3: Создать `hooks/use-version-comparison.ts`

### ЭТАП 3: Компоненты
- [ ] Шаг 3.1: Создать `components/RevisionProgress.tsx`
- [ ] Шаг 3.2: Создать `components/RevisionStatus.tsx`
- [ ] Шаг 3.3: Создать `components/RevisionLogs.tsx`
- [ ] Шаг 3.4: Создать `components/VersionComparison.tsx`

### ЭТАП 4: Рефакторинг
- [ ] Шаг 4.1: Разбить `index.tsx` на компоненты
- [ ] Шаг 4.2: Создать `components/ScriptList.tsx`
- [ ] Шаг 4.3: Создать `components/ScriptPreview.tsx`

### ЭТАП 5: Утилиты
- [ ] Шаг 5.1: Создать `utils/version-diff.ts`

### ЭТАП 6: Backend
- [ ] Шаг 6.1: Создать API endpoints для прогресса

---

## 🎯 ИТОГОВАЯ СТРУКТУРА

```
client/src/pages/auto-scripts/
├── index.tsx (150 строк) ✅
│
├── components/
│   ├── ScriptList.tsx (120 строк) ✅
│   ├── ScriptPreview.tsx (180 строк) ✅
│   ├── RevisionProgress.tsx (150 строк) ✅
│   ├── RevisionStatus.tsx (100 строк) ✅
│   ├── RevisionLogs.tsx (150 строк) ✅
│   └── VersionComparison.tsx (180 строк) ✅
│
├── hooks/
│   ├── use-auto-scripts.ts (120 строк) ✅
│   ├── use-revision-progress.ts (150 строк) ✅
│   ├── use-revision-logs.ts (100 строк) ✅
│   └── use-version-comparison.ts (120 строк) ✅
│
└── utils/
    ├── revision-stages.ts (80 строк) ✅
    └── version-diff.ts (150 строк) ✅

server/routes/
└── conveyor-progress.routes.ts (150 строк) ✅
```

**Всего файлов:** 13  
**Максимальный размер:** 180 строк ✅  
**Средний размер:** ~130 строк ✅

---

**План готов!** 📚

